# 近代日本建筑探索馆

基于 Vue3 + Vite + Pinia + Ant Design Vue + TypeScript 开发的日本近代建筑探索网站。

## 📖 项目简介

本项目基于《通识：学问的门类》中的"近代日本建筑"章节，通过交互式网站展示日本近代建筑的发展历程、代表建筑与建筑师。从明治维新的西式建筑引入，到战后现代主义的蓬勃发展，再到当代建筑的前卫探索，一览日本建筑的发展脉络。

## 🛠️ 技术栈

- **框架**: Vue 3 (Composition API)
- **构建工具**: Vite
- **状态管理**: Pinia
- **UI 组件库**: Ant Design Vue
- **类型支持**: TypeScript
- **路由**: Vue Router
- **地图**: 高德地图 API

## 📁 项目结构

```
Japanese_architecture_1.0/
├── src/
│   ├── assets/
│   │   ├── data/          # 静态数据文件
│   │   │   ├── buildings.json    # 建筑数据
│   │   │   ├── architects.json    # 建筑师数据
│   │   │   └── quotes.json        # 名言数据
│   │   └── ...
│   ├── components/        # 通用组件
│   │   ├── Navbar.vue     # 导航栏
│   │   ├── BuildingCard.vue  # 建筑卡片
│   │   └── TimelineItem.vue  # 时间轴项
│   ├── views/             # 页面组件
│   │   ├── Home.vue       # 首页
│   │   ├── Timeline.vue   # 时间轴页
│   │   ├── Architect.vue  # 建筑师列表页
│   │   ├── ArchitectDetail.vue  # 建筑师详情页
│   │   ├── Map.vue        # 地图页
│   │   ├── Favorites.vue  # 收藏页
│   │   └── BuildingDetail.vue   # 建筑详情页
│   ├── stores/            # Pinia 状态管理
│   │   └── useBuildingStore.ts
│   ├── router/            # 路由配置
│   │   └── index.ts
│   ├── types/             # TypeScript 类型定义
│   │   └── index.ts
│   ├── styles/            # 全局样式
│   │   └── main.scss
│   ├── App.vue            # 根组件
│   └── main.ts            # 入口文件
├── public/                # 静态资源
├── index.html             # HTML 模板
├── package.json           # 依赖配置
├── tsconfig.json          # TypeScript 配置
├── vite.config.ts         # Vite 配置
└── README.md              # 项目文档
```

## 🚀 快速开始

### 1. 安装依赖

```bash
npm install
```

### 2. 配置高德地图 API Key

在 `src/views/Map.vue` 文件中，将 `AMAP_KEY` 替换为你的高德地图 API Key：

```typescript
const AMAP_KEY = "YOUR_AMAP_KEY"; // 请替换为你的高德地图API Key
```

获取 API Key 的方法：

1. 访问 [高德开放平台](https://lbs.amap.com/)
2. 注册账号并创建应用
3. 获取 Web 端（JS API）的 Key

### 3. 启动开发服务器

```bash
npm run dev
```

访问 `http://localhost:5173` 查看项目。

### 4. 构建生产版本

```bash
npm run build
```

构建产物将输出到 `dist` 目录。

### 5. 预览构建结果

```bash
npm run preview
```

## 📄 页面说明

### 首页 (Home)

- 项目介绍和特色说明
- 统计数据展示（建筑数量、建筑师数量、收藏数量）
- 建筑文化名言随机展示
- 精选建筑展示

### 时间轴页 (Timeline)

- 按年代顺序展示所有建筑
- 使用 Ant Design Timeline 组件
- 点击建筑卡片可查看详情

### 建筑师页 (Architect)

- 展示所有建筑师信息
- 网格布局，响应式设计
- 点击卡片查看建筑师详情及作品

### 地图页 (Map)

- 集成高德地图
- 显示所有建筑的地理位置
- 点击标记点查看建筑信息
- 侧边栏显示建筑列表，支持快速定位

### 收藏页 (Favorites)

- 展示用户收藏的建筑
- 使用 localStorage 持久化存储
- 支持取消收藏

### 建筑详情页 (BuildingDetail)

- 展示建筑的详细信息
- 包含描述、建筑师、位置等信息
- 支持收藏和查看地图

### 建筑师详情页 (ArchitectDetail)

- 展示建筑师的详细信息
- 包含简介、成就、代表作品等
- 可查看该建筑师的所有作品

## 🎨 设计特色

- **主题色**: 红、白、灰、墨黑，体现日本建筑的简洁与禅意
- **响应式设计**: 支持桌面端和移动端
- **过渡动画**: 页面切换和组件交互都有流畅的动画效果
- **统一风格**: 所有组件使用统一的圆角、阴影风格

## 📦 依赖说明

### 核心依赖

- `vue`: ^3.5.22
- `vue-router`: ^4.4.5
- `pinia`: ^2.1.7
- `ant-design-vue`: ^4.2.3
- `@amap/amap-jsapi-loader`: ^1.0.1

### 开发依赖

- `typescript`: ^5.6.3
- `vue-tsc`: ^2.0.6
- `vite`: ^7.1.7
- `@vitejs/plugin-vue`: ^6.0.1

## 🌐 部署

### GitHub Pages

1. 修改 `vite.config.ts` 中的 `base` 配置：

```typescript
export default defineConfig({
  base: "/your-repo-name/", // 替换为你的仓库名
  // ...
});
```

2. 构建项目：

```bash
npm run build
```

3. 将 `dist` 目录的内容推送到 GitHub Pages 分支。

### Vercel

1. 安装 Vercel CLI：

```bash
npm i -g vercel
```

2. 在项目根目录运行：

```bash
vercel
```

3. 按照提示完成部署。

## 📝 数据说明

项目使用静态 JSON 文件存储数据：

- `buildings.json`: 建筑数据，包含名称、年份、建筑师、位置、经纬度等信息
- `architects.json`: 建筑师数据，包含姓名、生卒年份、简介、作品列表等
- `quotes.json`: 建筑文化名言数据

## 🔧 自定义配置

### 修改主题色

在 `src/styles/main.scss` 中修改 CSS 变量：

```scss
:root {
  --ant-primary-color: #8b0000; // 修改为主色调
}
```

### 添加新建筑

在 `src/assets/data/buildings.json` 中添加新的建筑数据：

```json
{
  "id": 9,
  "name": "建筑名称",
  "year": 2020,
  "architect": "建筑师名"
  // ... 其他字段
}
```

## 📚 参考资料

- [Vue 3 文档](https://vuejs.org/)
- [Ant Design Vue 文档](https://antdv.com/)
- [Pinia 文档](https://pinia.vuejs.org/)
- [高德地图 API 文档](https://lbs.amap.com/api/javascript-api/summary)

## 📄 许可证

MIT License

## 👨‍💻 作者

基于《通识：学问的门类》中的"近代日本建筑"章节开发

---

如有问题或建议，欢迎提交 Issue 或 Pull Request。
